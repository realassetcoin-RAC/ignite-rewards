import React from 'react';
import AuthPopupDemo from '@/components/AuthPopupDemo';

/**
 * Test page to verify the new login and signup popup components
 * This page can be accessed at /auth-popup-test to see the popups in action
 */
const AuthPopupTest: React.FC = () => {
  return <AuthPopupDemo />;
};

export default AuthPopupTest;
